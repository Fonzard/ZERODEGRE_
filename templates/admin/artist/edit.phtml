<main id="edit-artist" class="admin-form">
    <h2>Modifier un artiste</h2>
    <form action="/ZERODEGRE_/admin/artist/edit&id=<?= $data['artist']->getId() ?>" method="post">
        <fieldset>
            <label for="artist-name">Nom de l'artiste:</label>
            <input type="text" name="artist-name" value="<?= $data['artist']->getName() ?>" required>
        </fieldset>
        <fieldset>
            <label for="artist-description">Description:</label>
            <textarea name="artist-description"><?= $data['artist']->getDescription() ?></textarea>
        </fieldset>
        <h3>Modifier Media</h3>
        <?php if (!empty($data['media'])) : ?>
            <fieldset>
                <label for="artist-url"> Url</label>
                <input type="text" name="artist-url" id="artist-url" value="<?= $data['media']->getUrl() ?>">
            </fieldset>
            <fieldset>
                <label for="artist-altText">Texte descriptif</label>
                <input type="text" name="artist-altText" id="artist-altText" placeholder="Texte alternatif" value="<?= $data['media']->getAltText() ?>"/>
                <input type="hidden" name="artistMediaId" value="<?= $data['media']->getId() ?>">
            </fieldset>
        <?php else : ?>
            <fieldset>
                <label for="artist-url"> Url</label>
                <input type="text" name="artist-url" id="artist-url">
            </fieldset>
            <fieldset>
                <label for="artist-altText">Texte descriptif</label>
                <input type="text" name="artist-altText" id="artist-altText" placeholder="Texte alternatif"/>
                <input type="hidden" name="artistMediaId" value="">
            </fieldset>
        <?php endif ; ?>
            <fieldset>
                <button type="submit"> Modifier le l'artiste </button>
            </fieldset>
        <input type="hidden" name="artist-edit-form" value="submit"/>
    </form>
</main>